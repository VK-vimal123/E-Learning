// demo-launcher.js — small optional helper for local development
// This file is intentionally minimal. To run the app, open `index.html` in your browser
// or start a local static server (example below).

console.log('E-Learning demo launcher loaded (JS)');

/*
  Quick tips to run locally:
  - Double-click `index.html` to open in your browser, or
  - Start a simple server from the project root (recommended for video embeds):
      python -m http.server 8000
    Then open: http://localhost:8000
*/
